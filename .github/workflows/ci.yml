name: Test Template Workflow

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  # Test 1: Basic workflow (all defaults)
  basic-test:
    name: Basic Workflow Test
    uses: nohaila1231/CI-NODE-PIPELINE/.github/workflows/node-template.yml@main
    with:
      package-manager: npm
      test-command: npm test
      build-command: npm run build

  # Test 2: With security scanning
  security-test:
    name: With Security Scanning
    uses: nohaila1231/CI-NODE-PIPELINE/.github/workflows/node-template.yml@main
    with:
      package-manager: npm
      test-command: npm test
      build-command: npm run build
      enable-security-scan: 'true'

  # Test 3: Matrix testing (multiple Node versions)
  matrix-test:
    name: Matrix Testing
    uses: nohaila1231/CI-NODE-PIPELINE/.github/workflows/node-template.yml@main
    with:
      package-manager: npm
      test-command: npm test
      build-command: npm run build
